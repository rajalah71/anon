load_libraries = function() {

# This script loads all the required libraries

# 1. For pipeline functionalities and sorting
library(dplyr)

# 2. For one hot encoding in the matrix_distance function
library(caret)

# 3. data.tables
library(data.table)

}

load_libraries()
